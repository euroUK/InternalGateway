package bank.internalgateway.processor.bootstrap;

import org.springframework.stereotype.Component;

@Component
public class StartupSeed {
    // Test processor does not seed Kafka automatically — events are published via demo API or test scenarios.
}
